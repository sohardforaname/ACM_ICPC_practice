/*#include <bits/stdc++.h>
using namespace std;
const int N = 3e1 + 5;
int f[N], d[N];
int dis[N];
int main()
{
    int n;
    while (scanf("%d", &n) && n)
    {
        int h;
        scanf("%d", &h);
        for (int i = 1; i <= n; ++i)
            scanf("%d", &f[i]);
        for (int i = 1; i <= n; ++i)
            scanf("%d", &d[i]);
        for (int i = 1; i < n; ++i)
            scanf("%d", &dis[i]);
    }
}*/

