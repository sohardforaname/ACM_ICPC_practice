#include <bits/stdc++.h>
#include <bits/extc++.h>
using namespace std;
using namespace __gnu_pbds;
const int N = 1e5 + 5;
int a[N];
struct segtree
{
#define ls (k << 1)
#define rs ((k << 1) | 1)

    void build(int l, int r, int k)
    {
        if (l == r)
            ;
    }
};