#include <iostream>
#include <cstring>
#include <algorithm>
using namespace std;
int bot[1005];
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int n, h;
	cin >> n >> h;
	for (int i = 0; i < n; ++i)
		cin >> bot[i];
	
}