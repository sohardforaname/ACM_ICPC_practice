#include <bits/stdc++.h>
using namespace std;
int num[100005];
int len[10];
void cal_len(int x)
{
    int pos=0;
    while(x)
        x/=10,++pos;
    ++len[pos];
}
long long cal(int x)
{
    long long ans=0,tmp,t=1;
    while(x)
    {

    }
}

