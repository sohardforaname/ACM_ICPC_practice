#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    scanf("%d", &T);
    while (T--)
    {
        int ac, ad, bc, bd;
        scanf("%d%d%d%d", &ac, &ad, &bc, &bd);
    }
    return 0;
}