#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 5;
int a[N], sum[N];
bool check(int h, int n)
{
    memset(a, 0, sizeof(int) * (n + 2));
}
int main()
{
    int n;
    scanf("%d", &n);
    for (int i = 1; i <= n; ++i)
        scanf("%d", &a[i]);
}