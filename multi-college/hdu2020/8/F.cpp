#include <bits/stdc++.h>
using namespace std;
const int N = 2e5 + 5;
int l[N], r[N];
int maxn[N], minn[N];
void solve()
{
    int n, k;
    scanf("%d%d", &n, &k);
    for (int i = 1; i <= n; ++i)
        scanf("%d%d", &l[i], &r[i]);
}
